// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.20.0
// source: manual_credit.sql

package queries

import (
	"context"

	"github.com/google/uuid"
)

const recordManualCreditInflow = `-- name: RecordManualCreditInflow :one
insert into ledger.flow (
    id,
    type,
    metadata,
    twitch_user_id,
    delta_points,
    created_at,
    finalized_at,
    accepted
) values (
    gen_random_uuid(),
    'manual-credit',
    jsonb_build_object('note', $1::text),
    $2,
    $3,
    now(),
    now(),
    true
)
returning flow.id
`

type RecordManualCreditInflowParams struct {
	Note              string
	TwitchUserID      string
	NumPointsToCredit int32
}

func (q *Queries) RecordManualCreditInflow(ctx context.Context, arg RecordManualCreditInflowParams) (uuid.UUID, error) {
	row := q.db.QueryRowContext(ctx, recordManualCreditInflow, arg.Note, arg.TwitchUserID, arg.NumPointsToCredit)
	var id uuid.UUID
	err := row.Scan(&id)
	return id, err
}
